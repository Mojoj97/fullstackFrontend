dist: xenial
sudo: false

language: node_js
node_js:
  - "10"

cache:
  directories:
     - ./node_modules

install:
  - npm install

script:
  - npm run-script build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: a02a023026c04d36f77fdcb1b7b3c6d795edc840
  local_dir: dist/angular-github-pages-travis
  on:
    branch: master
